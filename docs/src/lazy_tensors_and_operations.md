# LazyTensors and operations

{What are LazyTensors? (Lazy, matrix free representation of a linear operator on multi dimensional arrays)}

{What is a TensorApplication? (A lazy representation of a tensor applied to an array. Computation happens when indexing into the result)}

{What operations are available: +, -, *, ∘, '}
{The operations are lazy...}
{The operations include some basic simplifications...}

{For full control of the computation you can use the corresponding constructors directly (TensorSum, TensorApplication, TensorComposition). These do not make any simplifications}
